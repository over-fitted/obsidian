- Bean = component of web app, wired by spring through dependency injection
	- historically use XML to wire
	- now use @Configuration class
		- only if spring cannot automatically detect where to inject